# 光标停留超过 500ms 后，再发生较大跳跃时显示拖尾 现在是10
cursor_trail 10

# 拖尾颜色；kitty 文档示例是 #rrggbb 或颜色名
cursor_trail_color #0099ff

# 两个值用空格分开，不要写括号和逗号
cursor_trail_decay 0.1 0.5

# 这里是“移动多少个字符格(cell)”才触发，不是像素
cursor_trail_start_threshold 3

background_opacity 0.75
background_blur 1
